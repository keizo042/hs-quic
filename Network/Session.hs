module Network.QUIC.Session
  (
    newSession
  , closeSession
  )
  where

import           Network.QUIC.Types

newSession :: IO Session
newSession = undefined

closeSession :: Session -> IO ()
closeSession = undefined
